embedding_size: 10                # (int) The embedding size of features.
# mlp_hidden_size: [128, 128, 128]  # (list of int) The hidden size of MLP layers.  
dropout_prob: 0.2                 # (float) The dropout rate.
n_layers: 2                     # (int) The number of GCN layers.
device: cuda:3