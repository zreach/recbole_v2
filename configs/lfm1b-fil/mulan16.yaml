external_features:
  item_feat: # 目标特征表 (可以是 'item_feat', 'user_feat', 'inter_feat')
    filepath: dataset/lfm1b-fil/kmeans/lfm1b-mulan-clusters-16.item # 外部特征文件路径
    on_col: tracks_id # 用于合并的列名
    external_feats: [cluster-16] # 外部特征列名列表